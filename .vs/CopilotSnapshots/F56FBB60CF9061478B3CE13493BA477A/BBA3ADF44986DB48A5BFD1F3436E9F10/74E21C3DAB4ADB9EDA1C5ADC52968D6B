"use client";

import { motion } from "framer-motion";
import {
  VideoIcon,
  Film,
  Scissors,
  ArrowUpCircle,
  Mic,
  Sun,
} from "lucide-react";

const videoTools = [
  {
    title: "Video Generator",
    description: "Generate stunning videos from text prompts using AI models.",
    icon: VideoIcon,
    color: "from-orange-500 to-amber-500",
    iconBg: "bg-orange-500/10",
    iconColor: "text-orange-500",
    href: "/video/generator",
  },
  {
    title: "Video Project Editor",
    description: "Edit and compose multi-scene video projects with a timeline.",
    icon: Film,
    color: "from-blue-500 to-cyan-500",
    iconBg: "bg-blue-500/10",
    iconColor: "text-blue-500",
    href: "/video/editor",
  },
  {
    title: "Clip Editor",
    description: "Trim, cut, and rearrange video clips with precision tools.",
    icon: Scissors,
    color: "from-pink-500 to-rose-500",
    iconBg: "bg-pink-500/10",
    iconColor: "text-pink-500",
    href: "/video/clip-editor",
  },
  {
    title: "Video Upscaler",
    description: "Enhance video resolution and quality with AI upscaling.",
    icon: ArrowUpCircle,
    color: "from-emerald-500 to-green-500",
    iconBg: "bg-emerald-500/10",
    iconColor: "text-emerald-500",
    href: "/video/upscaler",
  },
  {
    title: "Speak",
    description: "Add AI-generated voiceovers and lip-sync to your videos.",
    icon: Mic,
    color: "from-violet-500 to-purple-500",
    iconBg: "bg-violet-500/10",
    iconColor: "text-violet-500",
    href: "/video/speak",
  },
  {
    title: "Video Relight",
    description: "Adjust lighting and atmosphere in your videos with AI.",
    icon: Sun,
    color: "from-yellow-500 to-orange-400",
    iconBg: "bg-yellow-500/10",
    iconColor: "text-yellow-500",
    href: "/video/relight",
  },
];

const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: { staggerChildren: 0.08 },
  },
};

const cardVariants = {
  hidden: { opacity: 0, y: 20 },
  visible: { opacity: 1, y: 0 },
};

const VideoPage = () => {
  return (
    <div className="min-h-screen px-4 py-8 sm:px-6 lg:px-8">
      {/* ── Page Header ──────────────────────────────────────────────── */}
      <div className="mx-auto max-w-7xl">
        <div className="flex items-center gap-4 mb-2">
          <div className="flex h-12 w-12 items-center justify-center rounded-xl bg-orange-500/10">
            <VideoIcon className="h-7 w-7 text-orange-500" />
          </div>
          <div>
            <h1 className="text-2xl font-bold text-white sm:text-3xl">
              Video Studio
            </h1>
            <p className="text-sm text-zinc-400 mt-0.5">
              Create, edit, and enhance videos with AI-powered tools
            </p>
          </div>
        </div>

        {/* ── Decorative divider ─────────────────────────────────────── */}
        <div className="mt-6 mb-8">
          <div className="h-px w-full bg-gradient-to-r from-transparent via-orange-500/30 to-transparent" />
        </div>

        {/* ── Tools Grid ─────────────────────────────────────────────── */}
        <motion.div
          className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3"
          variants={containerVariants}
          initial="hidden"
          animate="visible"
        >
          {videoTools.map((tool) => (
            <motion.div
              key={tool.title}
              variants={cardVariants}
              whileHover={{ scale: 1.04, y: -4 }}
              whileTap={{ scale: 0.98 }}
              transition={{ type: "spring", stiffness: 300, damping: 20 }}
              className="group relative cursor-pointer"
            >
              <div className="relative overflow-hidden rounded-2xl border border-white/[0.06] bg-white/[0.03] p-6 backdrop-blur-sm transition-colors duration-300 hover:border-white/[0.12] hover:bg-white/[0.06]">
                {/* Gradient glow on hover */}
                <div
                  className={`absolute -inset-px rounded-2xl bg-gradient-to-br ${tool.color} opacity-0 blur-xl transition-opacity duration-500 group-hover:opacity-10`}
                />

                {/* Content */}
                <div className="relative z-10">
                  <div className={`mb-4 flex h-12 w-12 items-center justify-center rounded-xl ${tool.iconBg} transition-transform duration-300 group-hover:scale-110`}>
                    <tool.icon className={`h-6 w-6 ${tool.iconColor}`} />
                  </div>
                  <h3 className="mb-1.5 text-lg font-semibold text-white">
                    {tool.title}
                  </h3>
                  <p className="text-sm leading-relaxed text-zinc-400 group-hover:text-zinc-300 transition-colors">
                    {tool.description}
                  </p>
                </div>

                {/* Bottom accent line */}
                <div
                  className={`absolute bottom-0 left-0 right-0 h-[2px] bg-gradient-to-r ${tool.color} opacity-0 transition-opacity duration-300 group-hover:opacity-60`}
                />
              </div>
            </motion.div>
          ))}
        </motion.div>
      </div>
    </div>
  );
};

export default VideoPage;
    )
}


export default VideoPage;


