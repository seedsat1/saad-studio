# Saad Studio CEP client — env vars consumed by Vite at build time.
# Copy to `.env` (or `.env.local`) and adjust as needed.

# Base URL of the Next.js backend. The plugin hits /api/panel/* under this host.
VITE_SAAD_API=https://saadstudio.app
