const { PrismaClient } = require(\ @prisma/client\\); const prisma = new PrismaClient(); async function main() { const updates = [ { id: \\seedance2\\, data: { userCreditsRate: 4.5333, kieCredits: 41.0 } }, { id: \\seedance2f\\, data: { userCreditsRate: 6.0, kieCredits: 33.0 } }, { id: \\kling30\\, data: { userCreditsRate: 2.5, kieCredits: 14.0 } } ]; for (const u of updates) { const res = await prisma.pricingConstitution.update({ where: { id: u.id }, data: u.data }); console.log(\\Updated \\ + u.id); } } main().catch(console.error).finally(() => prisma.\$disconnect());
